# uni-pay-api Skill 使用手册

## 概述

`uni-pay-api` 是汇聚支付(JoinPay)聚合支付 API 接入助手，基于 **五能力架构** 设计，覆盖微信、支付宝、银联三大主流支付渠道，支持 MD5 和 RSA 双签名方式。

**适用场景**：接入汇聚支付聚合支付、生成接口代码示例、查阅业务知识、评估接入质量、排查支付问题。

---

## 目录结构

```
uni-pay-api/
├── SKILL.md                              # Skill 定义文件（AI Agent 读取）
├── 使用手册.md                            # 本文件 — 人工使用指南
├── config.json                           # 商户配置
├── scripts/                              # 可执行脚本
│   ├── uni_pay_client.py                 # 主 API 客户端（MD5签名，支持7个接口）
│   ├── query_order.py                    # 订单排障脚本
│   └── query_refund.py                   # 退款排障脚本
└── references/                           # 参考文档 & 代码示例
    ├── 1-产品选型/
    │   └── 支付渠道与交易类型对比.md
    ├── 2-示例代码/
    │   ├── 接口索引.md                   # 四语言 × 7接口 映射表
    │   ├── Java/                         # Java SDK + 7个接口示例
    │   ├── Python/                       # Python SDK + 接口示例
    │   ├── Go/                           # Go SDK + 客户端
    │   └── PHP/                          # PHP SDK + 客户端
    ├── 3-接入指南/
    │   ├── 开发必要参数说明.md
    │   ├── 签名与验签规则.md
    │   ├── 订单状态流转.md
    │   ├── 回调通知处理.md
    │   ├── 退款与分账规则.md
    │   └── 接入质量检查清单.md
    └── 4-问题排查/
        ├── 排障手册.md
        ├── 基础支付常见问题.md
        └── 响应码完整列表.md
```

---

## 五大能力说明

| 能力 | 用途 | 对应文档 |
|------|------|---------|
| **1. 产品选型** | 根据业务场景推荐支付方式，确定 FrpCode | `1-产品选型/支付渠道与交易类型对比.md` |
| **2. 示例代码** | 四语言(Java/Python/Go/PHP)接口调用代码 | `2-示例代码/` 目录 |
| **3. 业务知识速查** | 参数说明、签名算法、状态流转、回调、退款 | `3-接入指南/` 目录 |
| **4. 接入质量评估** | 签名验签、业务逻辑、回调处理规范性检查 | `3-接入指南/接入质量检查清单.md` |
| **5. 问题排查** | 响应码速查、FAQ、排障脚本 | `4-问题排查/` 目录 |

---

## 快速开始

### 1. 配置商户信息

编辑 `config.json`，填入你的汇聚支付商户信息：

```json
{
  "default_merchant_no": "你的商户编号",
  "merchant_key": "你的32位MD5密钥",
  "rsa_private_key": "RSA私钥内容（仅RSA签名需要）",
  "rsa_public_key": "RSA公钥内容（仅RSA签名需要）",
  "base_url": "https://trade.joinpay.cc",
  "default_notify_url": "你的回调通知地址",
  "default_trade_merchant_no": "报备商户号（微信渠道必填）",
  "sign_type": "MD5"
}
```

> **签名方式选择**：`sign_type` 可选 `"MD5"` 或 `"RSA"`。MD5 使用 `merchant_key`，RSA 使用 `rsa_private_key` / `rsa_public_key`。

### 2. 使用 CLI 脚本调用 API

脚本位于 `scripts/` 目录，使用 Python3 运行。

#### 统一下单（MD5签名）

```bash
# 微信扫码支付
python3 scripts/uni_pay_client.py pay \
  --order-no "ORD20260413001" \
  --amount "0.01" \
  --product-name "测试商品" \
  --frp-code "WEIXIN_NATIVE"

# 支付宝被扫
python3 scripts/uni_pay_client.py pay \
  --order-no "ORD20260413002" \
  --amount "1.00" \
  --product-name "线下收款" \
  --frp-code "ALIPAY_CARD"

# 银联APP支付
python3 scripts/uni_pay_client.py pay \
  --order-no "ORD20260413003" \
  --amount "100.00" \
  --product-name "大额支付" \
  --frp-code "UNIONPAY_APP"
```

#### 查询订单

```bash
python3 scripts/uni_pay_client.py query --order-no "ORD20260413001"
```

#### 申请退款

```bash
python3 scripts/uni_pay_client.py refund \
  --order-no "ORD20260413001" \
  --refund-order-no "REF20260413001" \
  --refund-amount "0.01" \
  --reason "用户申请退款"
```

#### 其他命令

```bash
# 退款查询
python3 scripts/uni_pay_client.py query_refund --refund-order-no "REF20260413001"

# 退款信息查询（按原订单号）
python3 scripts/uni_pay_client.py query_refund_info --order-no "ORD20260413001"

# 关闭订单
python3 scripts/uni_pay_client.py close --order-no "ORD20260413001" --frp-code "WEIXIN_NATIVE"

# 资金查询
python3 scripts/uni_pay_client.py query_funds --order-no "ORD20260413001"
```

### 3. 使用排障脚本

```bash
# 查询订单状态（含结果分析）
python3 scripts/query_order.py --merchant-no "888100500008456" --key "你的密钥" --order-no "ORD20260413001"

# 查询退款状态（含结果分析）
python3 scripts/query_refund.py --merchant-no "888100500008456" --key "你的密钥" --order-no "ORD20260413001"
```

---

## FrpCode 速查表

| 支付渠道 | 支付方式 | FrpCode |
|---------|---------|---------|
| **微信** | 扫码支付(主扫) | `WEIXIN_NATIVE` |
| **微信** | 付款码支付(被扫) | `WEIXIN_CARD` |
| **微信** | APP支付 | `WEIXIN_APP` / `WEIXIN_APP3` |
| **微信** | H5支付 | `WEIXIN_H5` / `WEIXIN_H5+` |
| **微信** | 公众号支付 | `WEIXIN_GZH` |
| **微信** | 小程序支付 | `WEIXIN_XCX` |
| **微信** | 收银台 | `WEIXIN_SYT` |
| **支付宝** | 扫码支付(主扫) | `ALIPAY_NATIVE` |
| **支付宝** | 付款码支付(被扫) | `ALIPAY_CARD` |
| **支付宝** | APP支付 | `ALIPAY_APP` |
| **支付宝** | H5支付 | `ALIPAY_H5` |
| **支付宝** | 服务窗支付 | `ALIPAY_FWC` |
| **支付宝** | 收银台 | `ALIPAY_SYT` |
| **银联** | 扫码支付(主扫) | `UNIONPAY_NATIVE` |
| **银联** | 付款码支付(被扫) | `UNIONPAY_CARD` |
| **银联** | APP支付 | `UNIONPAY_APP` |
| **银联** | H5支付 | `UNIONPAY_H5` |
| **银联** | 收银台 | `UNIONPAY_SYT` |

> 完整对比含各方式额外必填参数 → 参阅 `references/1-产品选型/支付渠道与交易类型对比.md`

---

## 签名算法要点

### MD5 签名

```
1. 排除 hmac 字段
2. 将所有参数按 key 字母升序排序
3. 拼接所有 value（不含 key）
4. hmac = MD5(value拼接串 + merchantKey).toUpperCase()
```

### RSA 签名

```
1. 同 MD5 步骤 1-3 生成 value 拼接串
2. 使用商户 RSA 私钥对拼接串进行签名
3. 验签使用商户 RSA 公钥
```

> 详细的四语言签名实现代码 → 参阅 `references/3-接入指南/签名与验签规则.md` 和 `references/2-示例代码/` 各语言 SDK 工具类

---

## API 接口一览

| # | 接口 | 路径 | 说明 |
|---|------|------|------|
| 1 | 统一支付下单 | `/tradeRt/uniPay` | 创建支付订单，返回支付凭证 |
| 2 | 订单查询 | `/tradeRt/queryOrder` | 查询订单支付状态 |
| 3 | 退款申请 | `/tradeRt/refund` | 申请订单退款 |
| 4 | 退款查询 | `/tradeRt/queryRefund` | 查询退款处理状态 |
| 5 | 退款信息查询 | `/tradeRt/queryRefundInfo` | 按原订单号查询退款信息 |
| 6 | 关闭订单 | `/tradeRt/closeOrder` | 关闭未支付的订单 |
| 7 | 资金查询 | `/tradeRt/queryFundsControlOrder` | 查询资金管控状态 |

---

## 代码示例索引

| 接口 | Java | Python | Go | PHP |
|------|------|--------|----|----|
| 统一支付下单 | `Java/1-统一支付下单/` | `Python/1-统一支付下单/` | SDK 客户端调用 | SDK 客户端调用 |
| 订单查询 | `Java/2-订单查询/` | `Python/2-订单查询/` | 同上 | 同上 |
| 退款申请 | `Java/3-退款申请/` | `Python/3-退款申请/` | 同上 | 同上 |
| 退款查询 | `Java/4-退款查询/` | `Python/4-7_其他接口示例/` | 同上 | 同上 |
| 退款信息查询 | `Java/5-退款信息查询/` | 同上 | 同上 | 同上 |
| 关闭订单 | `Java/6-关闭订单/` | 同上 | 同上 | 同上 |
| 资金查询 | `Java/7-资金查询/` | 同上 | 同上 | 同上 |
| **SDK 工具类** | `Java/SDK工具类/` | `Python/SDK工具类/` | `Go/SDK工具类/` | `PHP/SDK工具类/` |

> 完整映射表 → 参阅 `references/2-示例代码/接口索引.md`

---

## 常见问题快速定位

| 现象 | 可能原因 | 排查步骤 |
|------|---------|---------|
| 返回 `签名验证失败` | 密钥错误/排序有误/编码问题 | 检查 merchant_key、参数排序、编码是否 UTF-8 |
| 下单成功但未收到回调 | notify_url 不可达/未正确返回 | 检查 URL 可达性、返回 `"ok"` 或 `"success"` |
| 退款提交失败 | 订单未支付/已全额退款/超时 | 先 query 订单状态确认 |
| 关闭订单失败 | 订单已支付/已关闭 | 先 query 订单状态确认 |
| 微信渠道提示 `appid不存在` | 报备商户号未正确配置 | 检查 tradeMerchantNo 参数 |

> 更多问题 → 参阅 `references/4-问题排查/` 目录

---

## 注意事项

1. **金额单位**：所有金额参数单位为 **元**，精确到小数点后两位（如 `0.01`、`100.50`）
2. **字符编码**：统一使用 **UTF-8** 编码
3. **接口地址**：生产环境 `https://trade.joinpay.com`，测试环境 `https://trade.joinpay.cc`
4. **签名前置**：任何 API 调用前必须先完成签名
5. **回调幂等**：回调通知可能重复发送，业务端须做幂等处理
6. **RSA 签名**：CLI 脚本暂仅支持 MD5，RSA 签名请参考代码示例中的 SDK 工具类
